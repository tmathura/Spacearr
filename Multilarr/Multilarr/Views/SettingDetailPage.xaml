<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Multilarr.ViewModels;assembly=Multilarr"
             mc:Ignorable="d"
             x:Class="Multilarr.Views.SettingDetailPage"
             Title="{Binding Title}" x:DataType="viewModels:SettingDetailViewModel">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Command="{Binding CancelCommand}" />
        <ToolbarItem Text="Update" Command="{Binding UpdateCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Computer Name:" FontSize="Medium" />
                <Entry Text="{Binding SettingLog.ComputerName}" d:Text="Computer name" FontSize="Small" />
                <Label x:Name="SettingLog_ComputerNameError" IsVisible="False" TextColor="Red" />
                <Label Text="Pusher App Id:" FontSize="Medium" />
                <Entry Text="{Binding SettingLog.PusherAppId}" d:Text="Pusher app id" FontSize="Small" />
                <Label x:Name="SettingLog_PusherAppIdError" IsVisible="False" TextColor="Red" />
                <Label Text="Pusher Key:" FontSize="Medium" />
                <Entry Text="{Binding SettingLog.PusherKey}" d:Text="Pusher key" FontSize="Small" />
                <Label x:Name="SettingLog_PusherKeyError" IsVisible="False" TextColor="Red" />
                <Label Text="Pusher Secret:" FontSize="Medium" />
                <Entry Text="{Binding SettingLog.PusherSecret}" d:Text="Pusher secret" FontSize="Small" />
                <Label x:Name="SettingLog_PusherSecretError" IsVisible="False" TextColor="Red" />
                <Label Text="Pusher Cluster:" FontSize="Medium" />
                <Entry Text="{Binding SettingLog.PusherCluster}" d:Text="Pusher cluster" FontSize="Small" />
                <Label x:Name="SettingLog_PusherClusterError" IsVisible="False" TextColor="Red" />
                <Label Text="Is Default:" FontSize="Medium" />
                <CheckBox IsChecked="{Binding SettingLog.IsDefault}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>